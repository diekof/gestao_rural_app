import 'package:flutter/material.dart'; class OperationsPage extends StatelessWidget { const OperationsPage({super.key}); @override Widget build(BuildContext context)=>Scaffold(appBar: AppBar(title: const Text('Operações Agrícolas')), body: const Center(child: Text('Listagem de operações')));} 
